spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/devtracker_test
    username: postgres
    password: test123
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: create-drop  # for test, auto-create tables
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    show-sql: false

  flyway:
    enabled: false  # Usually disable in test since schema auto-generated

server:
  port: 0  # random port for test environment

logging:
  level:
    root: WARN
